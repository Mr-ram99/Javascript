/*
Implement a custom EventEmitter in JavaScript with support for subscribing to events and emitting them.

function emitSuccess(...args) {
    console.log("Emitter class implemented", args);
}

const emitter = new EventEmitter();
const sub = emitter.subscribe("change", emitSuccess);
emitter.emit("change", 1,2,3,4);
sub.unsubscribe();
*/
